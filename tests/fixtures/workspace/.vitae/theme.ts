/**
 * Example theme.
 *
 * Deliberately tiny: everything unspecified comes from the tool's defaults, so
 * a theme file only ever states what you actually want changed.
 *
 * Lengths are DXA (1440 = 1 inch); `sizes` are half-points (19 = 9.5pt).
 */
export default {
  font: 'Calibri',
  sizes: { name: 32 },
};
